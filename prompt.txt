Setup an agent to build the intro screens for the vision menu.

and when the intro screens are built then,
We need to build a splash screen motion animation with remotion.
create a temporary remotion project just for this animation.
build the splash screen motiion animation, which starts from blank
screen and with animation, first the V from the logo comes in screen,
then M comes in , and then with the animation, it moves to their place
which is the top left corner, and the splash screen animation should
end with the first intro screen, so it should exactly blend with the
first intro screen, let's go!